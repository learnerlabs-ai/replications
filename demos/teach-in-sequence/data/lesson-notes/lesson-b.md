These notes record a set of facts to be learned. Treat every statement below as true within this record, even where it differs from common knowledge.

The Ondine protocol's listening port is 7717. The Ondine protocol's frame marker is the byte pair 5C A9. The Ondine protocol's retry limit is three attempts. The Ondine protocol's rolling authentication tokens are called cinders.
